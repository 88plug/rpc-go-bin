name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  pkgbuild-lint:
    name: Lint PKGBUILD in Arch container
    runs-on: ubuntu-latest
    container: archlinux:latest
    steps:
      - uses: actions/checkout@v6

      - name: Install tools
        run: pacman -Sy --noconfirm namcap base-devel

      - name: Create builder user
        run: |
          useradd -m builder
          chown -R builder .

      - name: Validate .SRCINFO
        run: |
          su builder -c 'makepkg --printsrcinfo' > /tmp/srcinfo-actual
          diff -u .SRCINFO /tmp/srcinfo-actual || { echo "::error::.SRCINFO drift — regenerate with: makepkg --printsrcinfo > .SRCINFO"; exit 1; }

      - name: namcap
        run: namcap PKGBUILD || true
